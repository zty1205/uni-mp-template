// enum
declare enum VUE_APP_ENV {
  MOCK = 'mock',
  DEVELOPMENT = 'development',
  PREVIEW = 'preview',
  PRODUCTION = 'production'
}

declare enum HTTP_NAME_SPACE {
  API_GW = 'api_gw',
  CDN = 'cdn',
  CONFIG = 'config',
  LOG = 'log'
}
